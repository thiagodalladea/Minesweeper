from game import Game
from board import Board
from home import Home

homeWindowSize = (500, 500)

home = Home(homeWindowSize)
home.initialize()

#boardSize = (9, 9)
#board = Board(boardSize)
#windowSize = (900, 900)
#game = Game(board, windowSize)
#game.run()